import { Component,inject,signal } from '@angular/core';import { RouterLink } from '@angular/router';import { AdminCoupon,AdminCouponService } from '../../core/coupons/admin-coupon.service';
@Component({standalone:true,imports:[RouterLink],template:`<section><p class="eyebrow">GUTSCHEINE</p><h2>Gutscheine</h2><p><a class="create" routerLink="/gutscheine/neu">Neuen Gutschein anlegen</a></p>@if(loading()){<p>Lädt …</p>}@for(coupon of coupons();track coupon.id){<article>@if(coupon.imageUrl){<img [src]="coupon.imageUrl" alt=""/>}<div><h3>{{coupon.title}}</h3><p>{{coupon.subtitle}}</p><small>{{coupon.isVisible?'Sichtbar':'Ausgeblendet'}}</small></div><a class="edit" [routerLink]="['/gutscheine',coupon.id]">Bearbeiten</a><button (click)="toggle(coupon)">{{coupon.isVisible?'Ausblenden':'Einblenden'}}</button></article>}</section>`,styles:[`:host{display:block;max-width:52rem}.eyebrow{color:var(--admin-primary);font-weight:700;font-size:.8rem}.create,.edit{display:inline-flex;min-height:2.8rem;align-items:center;padding:0 .9rem;border-radius:.45rem;font-weight:700;text-decoration:none;cursor:pointer}.create,button{background:var(--admin-primary);color:#fff;border:0}.edit{color:var(--admin-primary);border:1px solid var(--admin-primary)}article{display:flex;align-items:center;gap:1rem;margin-top:.75rem;padding:1rem;border:1px solid var(--admin-border);border-radius:.75rem;background:#fff}article div{flex:1}h3,p{margin:0}small,p{color:var(--admin-muted)}img{width:4rem;height:4rem;object-fit:cover;border-radius:.5rem}button{min-height:2.8rem;padding:0 .9rem;border-radius:.45rem;font-weight:700;cursor:pointer}`]}) export class AdminCouponsComponent{private readonly service=inject(AdminCouponService);protected readonly coupons=signal<readonly AdminCoupon[]>([]);protected readonly loading=signal(true);constructor(){void this.load();}private async load(){try{this.coupons.set(await this.service.list());}finally{this.loading.set(false);}}protected async toggle(c:AdminCoupon){await this.service.toggle(c);await this.load();}}
