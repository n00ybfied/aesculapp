server {
    listen 80;
    listen [::]:80;
    server_name admin.aesculapp.floatbox.at;

    root /var/www/html/aesculapp/admin;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
